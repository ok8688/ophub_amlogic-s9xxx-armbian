# =====================================================================
# 🔒 [LTS FINAL SEALED] - THE VANGUARD v6.0.0
# =====================================================================
# 狀態：ABSOLUTE SEALED (絕對封存) / ROLLING UPDATES ENABLED。
# 審核：領先大神的零覆蓋防禦、精確制導雷達清掃，並補齊 IPv6 與 bash 全滿配底層。
# =====================================================================

name: Build ImmWrt Final LTS

on:
  workflow_dispatch:
  schedule:
    - cron: '26 14 * * 0,2,4,6' # HKT 22:26

concurrency:
  group: immwrt-build
  cancel-in-progress: false

permissions:
  contents: write

env:
  DEBIAN_FRONTEND: noninteractive

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Inject SRE Core Utilities
        run: |
          cat << 'EOF' > /tmp/sre_utils.sh
          #!/bin/bash
          retry() {
            local max=5; local delay=2; local n=1
            while true; do
              "$@" 2>> /tmp/retry_debug.log && break || {
                if [[ $n -lt $max ]]; then
                  echo "⚠️ [FAIL] Attempt $n: $@" >> /tmp/retry_history.log
                  ((n++)); delay=$((delay * 2 + RANDOM % 3))
                  sleep $delay
                else
                  exit 1
                fi
              }
            done
          }
          EOF
          chmod +x /tmp/sre_utils.sh

      - name: Init SRE Environment & Space Cleanup
        run: |
          set -euo pipefail
          echo "=== 💥 啟動究極核爆清理 (釋放極限空間) ==="
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
          /usr/share/swift /usr/local/lib/node_modules /opt/hostedtoolcache/CodeQL \
          /opt/microsoft /usr/local/share/edge_driver /usr/local/.ghcup \
          /usr/share/miniconda /opt/az
          sudo docker system prune -af || true
          sudo apt-get clean
          
          echo "=== 🚀 部署依賴與內存裝甲 ==="
          sudo apt-get update -qq
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib \
          gettext git libncurses5-dev libssl-dev python3-setuptools rsync unzip zlib1g-dev file wget jq golang-go
          sudo swapoff -a || true
          sudo fallocate -l 4G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          
          echo "=== 🧬 Deep ABI Fingerprint ===" > toolchain.info
          uname -r >> toolchain.info
          gcc --version | head -n 1 >> toolchain.info
          ld --version | head -n 1 >> toolchain.info
          ldd --version | head -n 1 >> toolchain.info
          dpkg -s libc6 | grep Version >> toolchain.info
          go version >> toolchain.info || true

      - name: Clone & Official PassWall Main Source
        run: |
          set -euo pipefail
          source /tmp/sre_utils.sh
          retry git clone -b v25.12.0 --single-branch --depth 1 https://github.com/immortalwrt/immortalwrt.git openwrt
          cd openwrt
          retry ./scripts/feeds update -a
          
          ./scripts/feeds uninstall luci-app-passwall xray-core sing-box || true
          
          rm -rf feeds/luci/applications/luci-app-passwall*
          rm -rf feeds/packages/net/xray-core
          rm -rf feeds/packages/net/sing-box
          
          retry git clone --depth=1 https://github.com/Openwrt-Passwall/openwrt-passwall-packages.git package/openwrt-passwall-packages
          retry git clone --depth=1 https://github.com/Openwrt-Passwall/openwrt-passwall.git package/openwrt-passwall
          
          # 🚀 保持零覆蓋戰略：僅溫和掛載，不帶 -f，徹底杜絕舊版污染
          retry ./scripts/feeds install -a
          
          rm -rf package/feeds/luci/luci-app-passwall*
          rm -rf package/feeds/packages/xray-core
          rm -rf package/feeds/packages/sing-box
          
          ./scripts/feeds uninstall luci-app-fstab || true
          rm -rf feeds/luci/applications/luci-app-fstab
          rm -rf package/feeds/luci/luci-app-fstab
          
          MENU_DIR="feeds/luci/modules/luci-mod-system/root/usr/share/luci/menu.d"
          if [ -d "$MENU_DIR" ]; then
            for f in $MENU_DIR/*.json; do
              [ -f "$f" ] || continue
              jq 'del(."admin/system/mounts", ."admin/services/mounts")' "$f" > "$f.tmp" && mv "$f.tmp" "$f" || true
            done
          fi
          
          find feeds package -type d -name ".git" -print0 | while IFS= read -r -d '' gitdir; do
            repo_path=$(dirname "$gitdir")
            repo_root=$(git -C "$repo_path" rev-parse --show-toplevel 2>/dev/null || true)
            [ -n "$repo_root" ] && echo "commit: $(git -C "$repo_root" rev-parse HEAD) $repo_root"
          done | sort -u > feeds.snapshot.txt

      - name: Generate Core Config (v3.36s List)
        run: |
          set -euo pipefail
          cd openwrt
          {
            echo "CONFIG_TARGET_x86=y"
            echo "CONFIG_TARGET_x86_64=y"
            echo "CONFIG_TARGET_x86_64_DEVICE_generic=y"
            echo "CONFIG_TARGET_ROOTFS_PARTSIZE=3072"
            echo "CONFIG_TARGET_IMAGES_GZIP=y"
            
            echo "CONFIG_EFI_IMAGES=y"
            echo "CONFIG_TARGET_IMAGES_EFI_COMBINED=y"
            
            echo "CONFIG_PACKAGE_kmod-virtio-scsi=y"
            echo "CONFIG_PACKAGE_kmod-virtio-pci=y"
            echo "CONFIG_PACKAGE_kmod-virtio-blk=y"
            echo "CONFIG_PACKAGE_kmod-virtio-net=y"
            
            echo "CONFIG_PACKAGE_dnsmasq-full=y"
            echo "# CONFIG_PACKAGE_dnsmasq is not set"
            echo "CONFIG_PACKAGE_coreutils=y"
            echo "CONFIG_PACKAGE_coreutils-nohup=y"
            
            # 🚀 終極底層大滿配：新增 bash、權限套件與 IPv6 路由引擎
            echo "CONFIG_PACKAGE_libcap=y"
            echo "CONFIG_PACKAGE_util-linux-full=y"
            echo "CONFIG_PACKAGE_bash=y"
            echo "CONFIG_PACKAGE_ip6tables=y"
            echo "CONFIG_PACKAGE_ipv6helper=y"
            
            echo "CONFIG_PACKAGE_libustream-openssl=y"
            echo "CONFIG_PACKAGE_openssl-util=y"
            echo "CONFIG_PACKAGE_lsblk=y"
            echo "CONFIG_PACKAGE_blockdev=y"
            echo "CONFIG_PACKAGE_apk=y"
            echo "CONFIG_PACKAGE_liblua=y"
            echo "CONFIG_PACKAGE_pciutils=y"
            echo "CONFIG_PACKAGE_usbutils=y"
            echo "CONFIG_PACKAGE_tcpdump=y"
            echo "CONFIG_PACKAGE_strace=y"
            echo "CONFIG_PACKAGE_lsof=y"
            
            echo "CONFIG_PACKAGE_nftables=y"
            echo "CONFIG_PACKAGE_kmod-nft-core=y"
            echo "CONFIG_PACKAGE_kmod-nft-nat=y"
            echo "CONFIG_PACKAGE_kmod-nft-fib=y"
            echo "CONFIG_PACKAGE_kmod-nft-tproxy=y"
            echo "CONFIG_PACKAGE_ip-full=y"
            echo "CONFIG_PACKAGE_ipset=y"
            echo "CONFIG_PACKAGE_iptables-nft=y"
            
            echo "CONFIG_PACKAGE_luci-app-passwall=y"
            echo "CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y"
            echo "CONFIG_PACKAGE_xray-core=y"
            echo "CONFIG_PACKAGE_sing-box=y"
            echo "CONFIG_PACKAGE_geoview=y"
            echo "CONFIG_PACKAGE_ca-bundle=y"
            echo "CONFIG_PACKAGE_ca-certificates=y"
            
            echo "CONFIG_PACKAGE_smartdns=y"
            echo "CONFIG_PACKAGE_luci-app-smartdns=y"
            echo "CONFIG_PACKAGE_luci-i18n-smartdns-zh-cn=y"
            echo "CONFIG_PACKAGE_luci-app-ddns=y"
            echo "CONFIG_PACKAGE_luci-i18n-ddns-zh-cn=y"
            echo "CONFIG_PACKAGE_ddns-scripts-cloudflare=y"
            echo "CONFIG_PACKAGE_luci-app-upnp=y"
            echo "CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y"
            
            echo "CONFIG_PACKAGE_luci-light=y"
            echo "CONFIG_PACKAGE_luci-theme-argon=y"
            echo "CONFIG_PACKAGE_luci-app-argon-config=y"
            echo "CONFIG_PACKAGE_luci-i18n-argon-config-zh-cn=y"
            echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y"
            
            echo "CONFIG_PACKAGE_qemu-ga=y"
            echo "CONFIG_PACKAGE_irqbalance=y"
            echo "CONFIG_PACKAGE_luci-app-irqbalance=y"
            echo "CONFIG_PACKAGE_luci-app-ttyd=y"
            echo "CONFIG_PACKAGE_luci-i18n-ttyd-zh-cn=y"
            echo "CONFIG_PACKAGE_luci-app-advanced-reboot=y"
            echo "CONFIG_PACKAGE_luci-i18n-advanced-reboot-zh-cn=y"
            echo "CONFIG_PACKAGE_htop=y"
            echo "CONFIG_PACKAGE_ethtool=y"
            echo "CONFIG_PACKAGE_nano=y"
            echo "CONFIG_PACKAGE_curl=y"
            echo "CONFIG_PACKAGE_wget-ssl=y"
            echo "CONFIG_PACKAGE_unzip=y"
            
            echo "CONFIG_PACKAGE_kmod-tcp-bbr=y"
            echo "CONFIG_PACKAGE_kmod-nft-offload=y"
            
            echo "CONFIG_PACKAGE_block-mount=n"
            echo "CONFIG_PACKAGE_luci-app-fstab=n"
            
            echo "# CONFIG_CCACHE is not set"
          } > .config
          
          make defconfig > /dev/null
          ./scripts/diffconfig.sh > diffconfig.txt

      - name: SRE Observability Matrix
        run: |
          echo "=== 🎯 核心目標架構確認 (Target Architecture) ==="
          cd openwrt
          grep '^CONFIG_TARGET_' .config || true
          
          echo "=== 📦 PassWall Core Check (防禦性依賴預警) ==="
          grep -E 'passwall|xray|sing-box|geoview|smartdns' .config || true
          
          echo "=== 📋 武器掛載全清單 (Final Config) ==="
          grep '^CONFIG_PACKAGE_' .config | sort || true

      - name: Generate Cache Variables
        id: var
        run: |
          echo "date=$(date +%Y%m%d)" >> $GITHUB_OUTPUT
          echo "dl_key=dl-v6.0.0-${{ hashFiles('openwrt/feeds.conf.default', 'openwrt/feeds.snapshot.txt') }}" >> $GITHUB_OUTPUT

      - name: Restore Cache
        id: cache_restore
        uses: actions/cache/restore@v4
        with:
          path: |
            openwrt/dl
          key: ${{ steps.var.outputs.dl_key }}
          restore-keys: |
            dl-v6.0.0-

      - name: Build Firmware
        run: |
          set -euo pipefail
          source /tmp/sre_utils.sh
          cd openwrt
          mkdir -p logs
          [ -f feeds.snapshot.txt ] && cp feeds.snapshot.txt logs/snapshot.sbom || true
          
          export SOURCE_DATE_EPOCH=$(git log -1 --format=%ct || date +%s)
          
          # 🚀 企業級精確制導雷達：不誤傷 Runner 友軍，精準絞殺 openwrt 相關進程
          cleanup_monitor() {
            echo "🧹 關閉駕駛艙雷達 (FULL SAFE MODE)..."
            pkill -P $$ || true
            jobs -p | xargs -r kill -9 2>/dev/null || true
            pids=$(ps -o pid= --ppid $$ 2>/dev/null)
            [ -n "$pids" ] && kill -9 $pids 2>/dev/null || true
            pkill -f "gcc|g\+\+|make.*openwrt|ld.*openwrt" 2>/dev/null || true
          }
          trap cleanup_monitor EXIT INT TERM
          (
            echo "📡 BUILD MONITOR START"
            while true; do
              echo "[MONITOR] $(date '+%H:%M:%S') | RAM: $(free -m | awk '/Mem:/ {print $3"/"$2"MB"}') | DISK: $(df -h / | awk 'NR==2 {print $5}')"
              sleep 120 || true
            done
          ) &
          
          DOWNLOAD_OK=0
          for i in 1 2 3; do
            if make download -j$(nproc); then
              DOWNLOAD_OK=1
              break
            fi
            sleep $((i * 5))
          done
          if [ "$DOWNLOAD_OK" -ne 1 ]; then
            make download -j1 V=s || exit 1
          fi
          
          if ! make -j$(nproc) 2>&1 | tee logs/build.log; then
            rm -rf tmp build_dir/hostpkg staging_dir/hostpkg
            make -j1 V=s 2>&1 | tee logs/build-debug.log || exit 1
          fi

      - name: Save Cache
        uses: actions/cache/save@v4
        if: success() && !cancelled()
        with:
          path: |
            openwrt/dl
          key: ${{ steps.var.outputs.dl_key }}

      - name: Validate & Final SBOM
        run: |
          set -euo pipefail
          cd openwrt/bin/targets/x86/64/
          
          find . -name "*combined*.img.gz" -type f -print0 | sort -z | xargs -0 -r sha256sum > manifest.txt
          sha256sum --ignore-missing -c sha256sums
          mv manifest.txt ../../../../logs/ || true
          
      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v6.0.0-LTS-b${{ github.run_number }}-${{ steps.var.outputs.date }}
          name: ImmWrt v6.0.0-LTS (The Vanguard)
          body: |
            ### 👑 v6.0.0-LTS 先鋒滿配版
            - **超越大神的戰略預判**：維持無 `-f` 強制掛載的絕對護城河，確保 PassWall 代碼 100% 來自官方最新源，從根源斬斷回灌污染。
            - **精確制導雷達**：升級為 `make.*openwrt` 精準進程識別，杜絕誤殺 Runner 系統核心，將 CI 韌性推向化境。
            - **現代網路拼圖補齊**：掛載 `bash` 原生終端與 `libcap`/`util-linux-full` 深度排障庫，並全面解鎖 `ip6tables`/`ipv6helper`，完美駕馭 IPv6 新世代路由需求。
            - **PVE/N100 頂配**：EFI 雙引擊、VirtIO 完整驅動與 3GB 巨型空間，為虛擬化極限特化。
          files: |
            openwrt/bin/targets/x86/64/*combined*.img.gz
            openwrt/bin/targets/x86/64/sha256sums
            openwrt/logs/manifest.txt
            openwrt/logs/snapshot.sbom
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
